'use client';
import Link from 'next/link';import {useState} from 'react';import {ArrowLeft} from 'lucide-react';
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:''});const [error,setError]=useState('');const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setLoading(true);setError('');try{const response=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const body=await response.json();if(!response.ok)throw new Error(body.error||'Unable to create account');setDone(true)}catch(reason){setError(reason instanceof Error?reason.message:'Unable to create account')}finally{setLoading(false)}}return <main className="flex min-h-screen items-center justify-center px-5 py-12"><div className="w-full max-w-md"><Link href="/" className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-widest"><ArrowLeft size={15}/> Shop</Link><p className="mt-16 text-[10px] font-bold uppercase tracking-[.2em] text-[var(--accent)]">Dammy T &amp; Motion Wears</p><h1 className="display mt-3 text-5xl">Create your account.</h1>{done?<div className="mt-8 border border-[#c7d9c3] bg-[#edf6eb] p-5 text-sm">Account created. <Link href="/api/auth/signin" className="font-semibold underline">Sign in to continue.</Link></div>:<form onSubmit={submit} className="mt-8 grid gap-5"><label className="grid gap-2 text-xs">Full name<input required value={form.name} onChange={event=>setForm({...form,name:event.target.value})} className="border-b border-[var(--line)] bg-transparent py-3 outline-none"/></label><label className="grid gap-2 text-xs">Email<input required type="email" value={form.email} onChange={event=>setForm({...form,email:event.target.value})} className="border-b border-[var(--line)] bg-transparent py-3 outline-none"/></label><label className="grid gap-2 text-xs">Password<input required minLength={8} type="password" value={form.password} onChange={event=>setForm({...form,password:event.target.value})} className="border-b border-[var(--line)] bg-transparent py-3 outline-none"/></label>{error&&<p role="alert" className="bg-[#fff0ed] p-3 text-xs text-[#9a3d2c]">{error}</p>}<button disabled={loading} className="bg-[var(--ink)] py-4 text-[11px] font-bold uppercase tracking-widest text-white disabled:opacity-50">{loading?'Creating account...':'Create account'}</button><p className="text-center text-xs text-[#777]">Already registered? <Link href="/api/auth/signin" className="underline">Sign in</Link></p></form>}</div></main>}
