'use client';
import Image from 'next/image';import Link from 'next/link';import {ArrowLeft,Heart} from 'lucide-react';import {products,money} from '../../lib/products';import {useStore} from '../../lib/store';
export default function WishlistPage(){const {wishlist,toggleWishlist}=useStore();const saved=products.filter(product=>wishlist.includes(product.id));return <main className="min-h-screen"><header className="border-b border-[var(--line)] px-5 py-5 lg:px-10"><div className="mx-auto flex max-w-[1200px] justify-between"><Link href="/" className="text-sm font-semibold uppercase tracking-[.12em]">DAMMY T &amp; MOTION WEARS</Link><Link href="/" className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-widest"><ArrowLeft size={15}/> Shop</Link></div></header><section className="mx-auto max-w-[1200px] px-5 py-12 lg:px-10"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-[var(--accent)]">Saved pieces</p><h1 className="display mt-3 text-5xl">Your wishlist.</h1>{saved.length===0?<p className="mt-12 border-t border-[var(--line)] py-12 text-sm text-[#777]">Save pieces while you browse and they will appear here.</p>:<div className="mt-10 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{saved.map(product=><div key={product.id}><Link href={`/product/${product.id}`}><div className="relative aspect-[.78] bg-[#e7e3dc]"><Image src={product.image} alt={product.name} fill className="object-cover"/></div><p className="mt-3 text-sm font-semibold">{product.name}</p><p className="mt-1 text-xs text-[#777]">{money(product.price)}</p></Link><button onClick={()=>toggleWishlist(product.id)} className="mt-3 text-[10px] font-bold uppercase tracking-widest text-[var(--accent)]">Remove</button></div>)}</div>}</section></main>}
