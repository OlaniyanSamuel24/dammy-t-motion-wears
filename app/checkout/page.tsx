'use client';
import Link from 'next/link';import {ArrowLeft,LockKeyhole} from 'lucide-react';import {useEffect,useState} from 'react';import {useStore} from '../../lib/store';import {money} from '../../lib/products';
export default function Checkout(){const {cart}=useStore();const [paid,setPaid]=useState(false);const [loading,setLoading]=useState(false);const [error,setError]=useState('');const subtotal=cart.reduce((sum,item)=>sum+item.price*item.quantity,0);const delivery=subtotal>=150000?0:3500;useEffect(()=>{const reference=new URLSearchParams(window.location.search).get('reference');if(!reference)return;setLoading(true);fetch(`/api/payments/verify?reference=${encodeURIComponent(reference)}`).then(async response=>{const body=await response.json();if(!response.ok)throw new Error(body.error||'Payment could not be verified');setPaid(true)}).catch(reason=>setError(reason instanceof Error?reason.message:'Payment verification failed')).finally(()=>setLoading(false))},[]);async function pay(){setError('');setLoading(true);try{const orderResponse=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:cart.map(item=>({productId:item.id,quantity:item.quantity,size:item.size})),address:{name:'Checkout customer',email:'',phone:'',address:'Nigeria'},deliveryFee:delivery})});const order=await orderResponse.json();if(!orderResponse.ok)throw new Error(order.error||'Please sign in before checking out');const paymentResponse=await fetch('/api/payments/initialize',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({orderId:order.id})});const payment=await paymentResponse.json();if(!paymentResponse.ok)throw new Error(payment.error||'Unable to start payment');window.location.assign(payment.authorizationUrl)}catch(reason){setError(reason instanceof Error?reason.message:'Payment could not be started')}finally{setLoading(false)}}if(paid)return <main className="flex min-h-screen items-center justify-center px-5 text-center"><div><p className="text-[10px] font-bold uppercase tracking-widest text-[var(--accent)]">Payment confirmed</p><h1 className="display mt-4 text-5xl">Thank you.</h1><p className="mt-4 text-sm text-[#666]">Your order is being prepared. We will send tracking updates to your account.</p><Link href="/account" className="mt-8 inline-block bg-[var(--ink)] px-6 py-4 text-[11px] font-bold uppercase tracking-widest text-white">Track your order</Link></div></main>;return <main className="min-h-screen"><header className="border-b border-[var(--line)] px-5 py-5 lg:px-10"><div className="mx-auto flex max-w-[1100px] justify-between"><Link href="/" className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-widest"><ArrowLeft size={16}/> Shop</Link><span className="display text-2xl tracking-[.18em]">DAMMY T &amp; MOTION WEARS</span><span className="flex items-center gap-2 text-[10px] uppercase tracking-widest text-[#777]"><LockKeyhole size={14}/> Secure checkout</span></div></header><div className="mx-auto grid max-w-[1100px] gap-12 px-5 py-12 md:grid-cols-[1fr_360px] lg:px-10"><section><h1 className="display text-4xl">Delivery details</h1><div className="mt-8 grid gap-5"><input className="border-b border-[var(--line)] bg-transparent px-0 py-3 text-sm outline-none" placeholder="Full name"/><input className="border-b border-[var(--line)] bg-transparent px-0 py-3 text-sm outline-none" placeholder="Email address" type="email"/><input className="border-b border-[var(--line)] bg-transparent px-0 py-3 text-sm outline-none" placeholder="Phone number"/><input className="border-b border-[var(--line)] bg-transparent px-0 py-3 text-sm outline-none" placeholder="Delivery address"/><select className="border-b border-[var(--line)] bg-transparent py-3 text-sm outline-none"><option>Lagos — ₦3,500</option><option>Abuja — ₦5,000</option><option>Port Harcourt — ₦5,000</option><option>Other Nigeria — ₦7,000</option></select></div>{error&&<p className="mt-5 bg-[#fff0ed] p-3 text-xs text-[#9a3d2c]">{error}</p>}<button onClick={pay} disabled={!cart.length||loading} className="mt-10 w-full bg-[var(--ink)] py-4 text-[11px] font-bold uppercase tracking-widest text-white disabled:opacity-40">{loading?'Processing...':'Pay securely with Paystack'}</button><p className="mt-3 text-center text-[10px] text-[#888]">Payment is verified securely before your order is marked paid.</p></section><aside className="h-fit border border-[var(--line)] p-6"><h2 className="text-[11px] font-bold uppercase tracking-widest">Order summary</h2>{cart.map(item=><div key={`${item.id}-${item.size}`} className="mt-5 flex justify-between gap-3 text-sm"><span>{item.name} <small className="text-[#888]">× {item.quantity}</small></span><span>{money(item.price*item.quantity)}</span></div>)}<div className="mt-6 border-t border-[var(--line)] pt-5 text-sm"><div className="flex justify-between"><span>Delivery</span><span>{money(delivery)}</span></div><div className="mt-4 flex justify-between font-semibold"><span>Total</span><span>{money(subtotal+delivery)}</span></div></div></aside></div></main>}
