'use client';
import Link from 'next/link';import {signIn} from 'next-auth/react';import {useState} from 'react';import {ArrowLeft} from 'lucide-react';
export default function SignIn(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setLoading(true);setError('');const result=await signIn('credentials',{email,password,redirect:false,callbackUrl:'/account'});if(result?.error)setError('Email or password is incorrect. Check your details or create an account.');else window.location.assign(result?.url||'/account');setLoading(false)}return <main className="flex min-h-screen items-center justify-center px-5 py-12"><div className="w-full max-w-md"><Link href="/" className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-widest"><ArrowLeft size={15}/> Shop</Link><p className="mt-16 text-[10px] font-bold uppercase tracking-[.2em] text-[var(--accent)]">Dammy T &amp; Motion Wears</p><h1 className="display mt-3 text-5xl">Welcome back.</h1><p className="mt-4 text-sm text-[#777]">Sign in to view your orders and complete checkout.</p><form onSubmit={submit} className="mt-8 grid gap-5"><label className="grid gap-2 text-xs">Email<input required type="email" autoComplete="email" value={email} onChange={event=>setEmail(event.target.value)} className="border-b border-[var(--line)] bg-transparent py-3 outline-none"/></label><label className="grid gap-2 text-xs">Password<input required minLength={8} type="password" autoComplete="current-password" value={password} onChange={event=>setPassword(event.target.value)} className="border-b border-[var(--line)] bg-transparent py-3 outline-none"/></label>{error&&<p role="alert" className="bg-[#fff0ed] p-3 text-xs text-[#9a3d2c]">{error}</p>}<button disabled={loading} className="bg-[var(--ink)] py-4 text-[11px] font-bold uppercase tracking-widest text-white disabled:opacity-50">{loading?'Signing in...':'Sign in'}</button><p className="text-center text-xs text-[#777]">New here? <Link href="/register" className="underline">Create an account</Link></p></form></div></main>}
