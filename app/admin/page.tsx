import Link from 'next/link';import {redirect} from 'next/navigation';import {BarChart3,PackageCheck,ShoppingCart,Users} from 'lucide-react';import {requireAdmin} from '../../lib/authz';
export default async function Admin(){try{await requireAdmin()}catch{redirect('/api/auth/signin?callbackUrl=/admin')}return <main className="min-h-screen"><header className="border-b border-[var(--line)] px-5 py-5 lg:px-10"><div className="mx-auto flex max-w-[1200px] items-center justify-between"><Link href="/" className="text-sm font-semibold uppercase tracking-[.12em]">DAMMY T &amp; MOTION WEARS</Link><span className="text-[10px] font-bold uppercase tracking-widest text-[#777]">Admin console</span></div></header><div className="mx-auto max-w-[1200px] px-5 py-12 lg:px-10"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-[var(--accent)]">Overview / Today</p><h1 className="display mt-3 text-4xl">Good morning, Admin.</h1><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Revenue','₦1,284,500',BarChart3],['Orders','48',ShoppingCart],['Active products','126',PackageCheck],['Sellers','32',Users]].map(([label,value,Icon])=><div key={label as string} className="border border-[var(--line)] p-5"><Icon size={19}/><p className="mt-8 text-xs text-[#777]">{label as string}</p><p className="mt-2 text-2xl font-semibold">{value as string}</p></div>)}</div></div></main>}
